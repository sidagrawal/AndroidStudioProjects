<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.phplogin.sidag.myapplication" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CreateNewUser"
            android:label="@string/title_activity_create_new_user" >
        </activity>
        <activity
            android:name=".MainListActivity"
            android:label="@string/title_activity_main_list"
            android:windowSoftInputMode="adjustPan">
        </activity>
        <activity
            android:name=".MainFragmentListActivity"
            android:label="@string/title_activity_main_fragment_list" >
        </activity>
        <activity
            android:name=".MainListViewActivity"
            android:label="@string/title_activity_main_list_view" >
        </activity>
    </application>

</manifest>
